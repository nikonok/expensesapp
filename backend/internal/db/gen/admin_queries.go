// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: queries.sql (admin section — Phase 10)

package gen

import (
	"context"
	"database/sql"
)

// ----- admin: user list -----

const listAdminUsers = `-- name: ListAdminUsers :many
SELECT
    u.id,
    u.email,
    u.display_name,
    u.last_signin_at,
    u.suspended_at,
    u.is_admin,
    u.is_root,
    u.promoter_id,
    u.delete_after,
    COALESCE(f.usage_bytes, 0)           AS usage_bytes,
    COUNT(DISTINCT CASE WHEN fm.left_at IS NULL THEN fm.user_id END) AS family_member_count,
    COUNT(DISTINCT d.id)                 AS device_count,
    CASE WHEN fre.family_id IS NOT NULL THEN 1 ELSE 0 END AS has_recovery_code
FROM users u
LEFT JOIN family_members fm2 ON fm2.user_id = u.id AND fm2.left_at IS NULL
LEFT JOIN families f          ON f.id = fm2.family_id
LEFT JOIN family_members fm   ON fm.family_id = fm2.family_id AND fm.left_at IS NULL
LEFT JOIN devices d            ON d.user_id = u.id AND d.status IN ('pending','active')
LEFT JOIN family_recovery_envelopes fre ON fre.family_id = fm2.family_id
GROUP BY u.id
ORDER BY u.created_at DESC
LIMIT ? OFFSET ?
`

type ListAdminUsersParams struct {
	Limit  int64
	Offset int64
}

type ListAdminUsersRow struct {
	ID               string
	Email            string
	DisplayName      string
	LastSigninAt     sql.NullString
	SuspendedAt      sql.NullString
	IsAdmin          int64
	IsRoot           int64
	PromoterID       sql.NullString
	DeleteAfter      sql.NullString
	UsageBytes       int64
	FamilyMemberCount int64
	DeviceCount      int64
	HasRecoveryCode  int64
}

func (q *Queries) ListAdminUsers(ctx context.Context, arg ListAdminUsersParams) ([]ListAdminUsersRow, error) {
	rows, err := q.db.QueryContext(ctx, listAdminUsers, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAdminUsersRow{}
	for rows.Next() {
		var i ListAdminUsersRow
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.DisplayName,
			&i.LastSigninAt,
			&i.SuspendedAt,
			&i.IsAdmin,
			&i.IsRoot,
			&i.PromoterID,
			&i.DeleteAfter,
			&i.UsageBytes,
			&i.FamilyMemberCount,
			&i.DeviceCount,
			&i.HasRecoveryCode,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

// ----- admin: audit log -----

const listAuditLog = `-- name: ListAuditLog :many
SELECT id, actor_user_id, actor_email, action, target_kind, target_id, detail_json, created_at
FROM audit_log
WHERE (? = '' OR id < ?)
ORDER BY id DESC
LIMIT ?
`

type ListAuditLogParams struct {
	AfterID string
	Limit   int64
}

func (q *Queries) ListAuditLog(ctx context.Context, arg ListAuditLogParams) ([]AuditLog, error) {
	rows, err := q.db.QueryContext(ctx, listAuditLog, arg.AfterID, arg.AfterID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditLog{}
	for rows.Next() {
		var i AuditLog
		if err := rows.Scan(
			&i.ID,
			&i.ActorUserID,
			&i.ActorEmail,
			&i.Action,
			&i.TargetKind,
			&i.TargetID,
			&i.DetailJson,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

// ----- admin: get promoter chain for subtree check -----

const getUserPromoterID = `-- name: GetUserPromoterID :one
SELECT promoter_id FROM users WHERE id = ? LIMIT 1
`

// GetUserPromoterID returns the promoter_id for a user. Used for subtree walks.
func (q *Queries) GetUserPromoterID(ctx context.Context, id string) (sql.NullString, error) {
	row := q.db.QueryRowContext(ctx, getUserPromoterID, id)
	var promoterID sql.NullString
	err := row.Scan(&promoterID)
	return promoterID, err
}

// ----- admin: device revoke (already exists via RevokeDevice / RevokeAllDeviceSessions) -----
// These are already defined in queries.sql.go — no new queries needed for device revoke.
